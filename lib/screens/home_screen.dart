// lib/screens/home_screen.dart
//
// This is the LEGACY home screen file (from lib/screens/).
// It now delegates to the real feature screen to avoid duplication.
// The router (app_router.dart) uses the feature-layer HomeScreen exclusively,
// so this file is kept only for backward compatibility with any old imports.

export '../features/products/presentation/screens/home_screen.dart';
